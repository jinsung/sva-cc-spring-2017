function setup () {
  createCanvas(640, 480);
}

function draw() {
  background (1
  if (mouseIsPressed) {
    fill(0);
  } else {
    fill(Math.random() * 255);
  }
  ellipse(mouseX, mouseY, 80, 80);
}
