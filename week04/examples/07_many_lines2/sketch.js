

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(100);
  var x1 = 10;
  var y1 = 10;
  var y2 = 100;
  var gap = 10;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
  x1 = x1 + gap;
  line(x1, y1, x1, y2);
}
