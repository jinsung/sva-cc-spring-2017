function Robot() {


  this.draw = function (isOn) {
    stroke(0);
    fill(100);
    rectMode(CENTER);
    ellipseMode(CENTER);
    this.drawHead();
    this.drawBody();
  }

  this.drawHead = function() {

  }

  this.drawBody = function () {

  }
}
